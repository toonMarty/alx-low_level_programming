#include "main.h"
/**
 * _abs- Entry point
 *
 * @n: a number to find its absolute value
 * Return: -n (Negative) and n (Positive)
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
